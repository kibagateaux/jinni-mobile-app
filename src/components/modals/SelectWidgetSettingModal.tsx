// check if widget.getOptions
// useMemo if getOptions and !options - setOptions(await getOptions())
// will be callable from home.addWidgets or item.addWdiget
// 1. player selects widget, popsup this modal, modal retrieves options,
// on saveWidget - call api with WidgetSettingInput
